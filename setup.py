from setuptools import setup, find_packages

setup(
    # Metadata
    name='my_test_pkg',
    install_requires=[
        'python>=3.8',
        'matplotlib>=3.3.2',
        'numpy >=1.18',
        'pandas >=1.1.2'
    ]

)
